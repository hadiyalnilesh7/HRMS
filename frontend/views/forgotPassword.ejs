<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forgot Password - HRMS</title>
  
  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-page">
  <div class="login-container">
    <div class="login-card">
      <div class="login-body text-center">
        <div class="mb-4">
          <i class="fa-solid fa-lock fa-3x text-primary opacity-50"></i>
        </div>
        <h2 class="mb-2">Forgot Password?</h2>
        <p class="text-muted mb-4">Enter your email address and we'll send you a link to reset your password.</p>
        
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger text-start">
            <i class="fa-solid fa-circle-exclamation me-1"></i> <%= error %>
          </div>
        <% } %>
        
        <% if (typeof message !== 'undefined' && message) { %>
          <div class="alert alert-success text-start">
            <i class="fa-solid fa-check-circle me-1"></i> <%= message %>
          </div>
        <% } %>
        
        <form method="POST" action="/forgot-password" class="text-start">
          <div class="form-group mb-4">
            <label for="email" class="form-label">Email Address</label>
            <div class="input-group">
              <span class="input-group-text bg-light border-end-0"><i class="fa-regular fa-envelope text-muted"></i></span>
              <input type="email" id="email" name="email" class="form-control border-start-0 ps-0" placeholder="Enter your email" required style="border-left: none;">
            </div>
          </div>

          <button type="submit" class="login-button">
            <i class="fa-solid fa-paper-plane me-2"></i> Send Reset Link
          </button>
        </form>
      </div>

      <div class="login-footer">
        <p class="mb-0">Remember your password? <a href="/login" class="fw-bold">Back to Login</a></p>
      </div>
    </div>

    <div class="back-home">
      <a href="/"><i class="fa-solid fa-arrow-left me-1"></i> Back to Home</a>
    </div>
  </div>
</body>
</html>
